import { StyleSheet } from "react-native";

const globalStyles = StyleSheet.create({
  button: { backgroundColor: "green" },

  contentButton: { backgroundColor: "blue" },
});

export default globalStyles;
